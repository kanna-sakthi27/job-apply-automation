# {{CHANNEL}} — answer / run log (channel copy)

> Copy to `answers.md`. **This file is a log, not a source of answers**: every form answer comes
> from `bank/core.md`. Keep it that way — runs read `bank/core.md` once and only *append* here,
> which is what keeps a run cheap.

## §1 — Channel notes

`<anything specific to this site worth remembering between runs>`

## §2 — Screener mapping

Map each screening question to a `bank/core.md` section. The bank already covers identity, work
authorization, compensation, numeric experience screeners and preferences.

| Screener keyword | Bank section |
|---|---|
| `<keyword>` | `<bank/core.md §n>` |

## §5 — Unanswered questions (append-only, newest first)

A required question with no bank answer is **not** a reason to abandon a job: ask the operator,
log it here, continue with other jobs.

| Date | Job | Question | Options | Status |
|---|---|---|---|---|
| | | | | |

## §6 — Run log (also the day-cap counter)

One row per job. Append only — never rewrite. Count of rows dated today marked `applied` is what
the pre-flight uses to compute the remaining budget.

```
date | title @ company, country | applied / skipped / needs-review | reason
```

| Date | Job | Outcome | Reason |
|---|---|---|---|
| | | | |

End every run with a single `RUN SUMMARY` row: found / applied / skipped and why.
